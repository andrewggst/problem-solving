name: Atualizar README da pasta beecrowd

on:
  push:
    paths:
      - 'beecrowd/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositórioname: Atualizar README da pasta beecrowd

on:
  push:
    paths:
      - 'beecrowd/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Configurar Git e autenticar push
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Remover branch local e criar novamente
        run: |
          git checkout --detach
          git branch -D main || echo "Branch main não existe localmente"
          git checkout -b main
          git fetch origin main
          git reset --hard origin/main
          git clean -fd

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Instalar dependências (se houver)
        run: pip install --upgrade pip

      - name: Gerar README.md da pasta beecrowd
        run: python .github/scripts/gerar_readme.py

      - name: Commit do novo README.md e push forçado
        run: |
          git add beecrowd/README.md
          git commit -m "🤖 Atualiza README da pasta beecrowd automaticamente" || echo "Nada para commitar"
          git push origin main --force

        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Instalar dependências (se necessário)
        run: pip install --upgrade pip

      - name: Configurar Git e sincronizar branch main
        run: |
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git fetch origin main
          git reset --hard origin/main
          git clean -fd
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Gerar README.md da pasta beecrowd
        run: python .github/scripts/gerar_readme.py

      - name: Commit do novo README.md
        run: |
          git fetch origin main
          git reset --hard origin/main
          git add beecrowd/README.md
          git commit -m "🤖 Atualiza README da pasta beecrowd automaticamente" || echo "Nada para commitar"
          git push origin main --force
